/**
 * API Configuration
 * Centralized backend URL configuration for frontend
 */

const API_BASE_URL = "http://localhost:8000";

export const apiConfig = {
  baseURL: API_BASE_URL,
};

export default apiConfig;
